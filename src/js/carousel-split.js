import { MAGICNUMBER } from './utils/dom-utils';

export default class CarouselSplit {
    constructor(element, options) {
        this.init();
    }

    init() {
        console.log(`El número mágico es: ${MAGICNUMBER}`);
    }
}
